inv:
  params:
    a: 601
    b: 602

# Propiedad para conectarse a una base de datos
spring:
  datasource:
    url: jdbc:h2:mem:producto-db
  h2:
    console:
      enabled: true
      path: /h2-console

# Propiedad para exponer TODOS los endpoints de monitoreo
management:
  endpoints:
    web:
      exposure:
        include: "*"